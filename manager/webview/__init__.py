# webview sub-package — Qt6+CEF local display and manager-side controller,
# integrated into the manager process (multiple webview instances can connect
# to a single manager: the local one plus any remote client)
